## What does this PR do?

## Checklist

- [ ] Tests cover the change (unit for pure logic, envtest for reconciler
      behavior) and `make coverage` passes locally
- [ ] `make lint` passes
- [ ] Docs updated if behavior or configuration changed
