apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - rbac.yaml
  - deployment.yaml

configMapGenerator:
  - name: loafer-config
    namespace: loafer-system
    files:
      - config.yaml
